{
  "id": "review-v2",
  "name": "Review (v2, on the crawl's building blocks)",
  "description": "(#2310 P4c/P4c-2, NOT the frozen `review` id) A code review built as a SECOND mission config on the crawl's shared blocks (#2301/#2310), never as a fork of it: `plan-<rule>` mints one `plan.sites` task per rule with `source: \"diff\"` (the DIFF-scoped strategy on the same plan_sites pattern `crawl.plan`'s tree walk uses — see plan_sites_step.rs); `unit-<rule>` GROWS one `crawl.unit` dispatch per planned site, `role_id: \"reviewer\"` (the SAME step kind crawl.json's `unit-<rule>` phase uses, generalized in this packet to read the Task's own role rather than a hardcoded `crawler`); `create-mods` reuses crawl's create-mod pattern (plus its own `gate-step`, #2310 P4c-2b) over this run's own findings; `deliver` (#2310 P4c-2b) gathers this run's own finding/mod stores and renders a GitHub review payload. Crawl never learns what a PR is (DESIGN.md): this document names no crawl id, and `crawl.json` is untouched by this packet.",
  "schema_version": "3.4",
  "inputs": [
    {
      "name": "workspace",
      "description": "Path to a workspace spec JSON naming the checked-out tree at the diff's own head — REQUIRED for every `plan-<rule>` step. Unlike the frozen `review` config's `worktree`/`github` inputs, a diff-scoped plan step reads through `workspace_spec::materialize`, the SAME mechanism `crawl.json` uses, so this run needs a real materialized source (a `path` source pointing at the checked-out tree is the common case — see the P4 brief's own live-plumbing-proof recipe, \"over a tempdir tree of the post-diff fixture\"). Must resolve to exactly ONE source.",
      "required": true
    },
    {
      "name": "diff_file",
      "description": "Path to the unified diff this review plans against. Canonicalized at step-config parse time.",
      "required": true
    },
    {
      "name": "intent_file",
      "description": "Path to the PR body / intent text `intent-vs-diff` compares the diff against. Mirrors the frozen `review` config's own input of the same name; NOT YET READ by any step in this document — the `intent-vs-diff` rule's window-only detection reads intent from its dispatch message once the unit-dispatch prompt is wired to it (out of this packet's scope; see the rule file's own comment).",
      "required": false
    },
    {
      "name": "head_sha",
      "description": "The diff's own head sha — mirrors the frozen `review` config's input. Reaches each `plan-<rule>` step's config but is not yet consumed (see `plan_sites_step.rs`'s module doc: `Plan.sources[0].sha` comes from the checked-out tree today, not this param).",
      "required": false
    },
    {
      "name": "github",
      "description": "owner/repo — mirrors the frozen `review` config's input. Reaches each `plan-<rule>` step's config but is not yet consumed (no fetch path exists yet — see `plan_sites_step.rs`'s module doc).",
      "required": false
    },
    {
      "name": "mode",
      "description": "Mirrors the frozen `review` config's input. Accepted for CLI-surface parity; not yet read by any step in this document.",
      "required": false
    },
    {
      "name": "bundler",
      "description": "Mirrors the frozen `review` config's input (the external-bundler plugin path). Accepted and IGNORED (#2310 P4c-2 item 4: declared \"ignored\" structurally, not detected by config id) — `review-v2` does not use the funnel's bundler; its planner reads `diff_file` directly. Passing it prints a named warning at launch time (before dispatch, on the `--dry-run` path too) saying it is ignored, so an operator carrying a `bundler=` param over from a `review` launch line gets a visible signal rather than a silent no-op.",
      "required": false,
      "ignored": true,
      "ignored_reason": "review-v2 does not use the funnel's bundler; its planner reads diff_file directly"
    },
    {
      "name": "envelope_out",
      "description": "Mirrors the frozen `review` config's input. Still NOT consumed (#2310 P4c-2b): `deliver.github_review` writes the rendered `{mode, review}` payload only (via `emit`), never a separate full envelope file — there is no envelope shape in this document's own pipeline for this to name.",
      "required": false
    },
    {
      "name": "emit",
      "description": "Path to write the rendered `{mode, review}` payload the `deliver` phase's `deliver-step` produces. `-` (or omitted) writes to stdout, matching the frozen `review` config's own `emit` contract.",
      "required": false
    },
    {
      "name": "attribution",
      "description": "Attribution text for the posted footer, threaded to the `deliver` phase's `deliver-step` — mirrors the frozen `review` config's own `attribution` input. Absent means no footer line (`deliver.github_review`'s own default), NOT the frozen config's envelope-derived default — this document has no member-record envelope to derive one from.",
      "required": false
    },
    {
      "name": "test_command",
      "description": "The gate command `create-mods` runs against a proposed mod before counting it confirmed (#2310 P4c — DESIGN.md \"the changed files name the test targets\"). Wired (#2310 P4c-2b): the `create-mod` task's `gate-step` (`mods.gate`) runs this command in `{{item.tree_root}}` against every mod naming the finding the task addresses, and records `gate_passed`/`gate_skipped_reason` on each. Leaving it unset means every mod is recorded `gate_skipped_reason: \"no test_command configured\"`, never gate-verified.",
      "required": false
    },
    {
      "name": "rules",
      "description": "Comma-separated rule ids to review with, same selection mechanism as `crawl.json`'s own `rules` input — a `plan-<rule>` task whose rule is not named is pruned at mint with reason `not_selected`. Absent means every enabled plan task this document declares.",
      "required": false
    },
    {
      "name": "draws",
      "description": "How many times to dispatch each planned unit (#2310 P4c-2b — DESIGN.md \"a draws-per-unit knob, off by default, ports the measured k-draw recall technique from the funnel\"). Absent means 1 (`crawl.unit`'s own default) — one dispatch per unit, unchanged from before this input existed. `finding_refs` dedup on `(rule, file, line)` across a unit's own draws before `create-mods` grows from them, so a duplicate observation across draws never mints two coder tasks for the same window.",
      "required": false
    },
    {
      "name": "review-probe-high",
      "description": "Seat override for the `unit-<rule>` tasks — same param name shape the P4 brief's live-plumbing-proof recipe uses (`--param review-probe-high=probe-4b`), kept even though this document has no probe/judge/verify staffing split of its own YET: a single seat resolves every `unit-<rule>` task via the `reviewer` role's own `role_profiles.reviewer` binding, or this override.",
      "required": false
    },
    {
      "name": "dry_run",
      "description": "The generic `mission launch --dry-run` flag, injected by the CLI layer. Prints the task/step graph this launch would mint and mints nothing.",
      "required": false
    }
  ],
  "phases": [
    {
      "id": "plan",
      "display_name": "Plan",
      "description": "One planning task per rule, diff-scoped. Each materializes `workspace`, reads `diff_file`, and writes plan/<rule>.json — the SAME `Plan` content shape `crawl.plan` writes (`plan.sites`'s tree-source path IS `crawl.plan`'s own `plan_one_rule`), just produced from a diff's hunks instead of a tree walk.",
      "tasks": [
        {
          "id": "plan-intent-vs-diff",
          "display_name": "Plan: Intent vs diff",
          "description": "Plan the `intent-vs-diff` rule over the diff.",
          "steps": [{"id": "plan-intent-vs-diff-step", "kind": "plan.sites", "config": {"rule": "intent-vs-diff", "workspace": "{{workspace}}", "source": "diff", "diff_file": "{{diff_file}}", "head_sha": "{{head_sha}}", "github": "{{github}}"}}]
        },
        {
          "id": "plan-existing-solution",
          "display_name": "Plan: Existing solution",
          "description": "Plan the `existing-solution` rule over the diff.",
          "steps": [{"id": "plan-existing-solution-step", "kind": "plan.sites", "config": {"rule": "existing-solution", "workspace": "{{workspace}}", "source": "diff", "diff_file": "{{diff_file}}", "head_sha": "{{head_sha}}", "github": "{{github}}"}}]
        },
        {
          "id": "plan-shared-symbol-callers",
          "display_name": "Plan: Shared symbol callers",
          "description": "Plan the `shared-symbol-callers` rule over the diff.",
          "steps": [{"id": "plan-shared-symbol-callers-step", "kind": "plan.sites", "config": {"rule": "shared-symbol-callers", "workspace": "{{workspace}}", "source": "diff", "diff_file": "{{diff_file}}", "head_sha": "{{head_sha}}", "github": "{{github}}"}}]
        },
        {
          "id": "plan-union-vs-enum",
          "display_name": "Plan: Union vs enum",
          "description": "Plan the `union-vs-enum` rule over the diff.",
          "steps": [{"id": "plan-union-vs-enum-step", "kind": "plan.sites", "config": {"rule": "union-vs-enum", "workspace": "{{workspace}}", "source": "diff", "diff_file": "{{diff_file}}", "head_sha": "{{head_sha}}", "github": "{{github}}"}}]
        },
        {
          "id": "plan-test-gap",
          "display_name": "Plan: Test gap",
          "description": "Plan the `test-gap` rule over the diff.",
          "steps": [{"id": "plan-test-gap-step", "kind": "plan.sites", "config": {"rule": "test-gap", "workspace": "{{workspace}}", "source": "diff", "diff_file": "{{diff_file}}", "head_sha": "{{head_sha}}", "github": "{{github}}"}}]
        },
        {
          "id": "plan-swallowed-error",
          "display_name": "Plan: Swallowed errors (from crawl)",
          "description": "Plan the crawl's `swallowed-error` rule over the diff — reused via its `scope: [\"tree\",\"diff\"]`.",
          "steps": [{"id": "plan-swallowed-error-step", "kind": "plan.sites", "config": {"rule": "swallowed-error", "workspace": "{{workspace}}", "source": "diff", "diff_file": "{{diff_file}}", "head_sha": "{{head_sha}}", "github": "{{github}}"}}]
        },
        {
          "id": "plan-unnamed-predicate",
          "display_name": "Plan: Unnamed predicates (from crawl)",
          "description": "Plan the crawl's `unnamed-predicate` rule over the diff — reused via its `scope: [\"tree\",\"diff\"]`.",
          "steps": [{"id": "plan-unnamed-predicate-step", "kind": "plan.sites", "config": {"rule": "unnamed-predicate", "workspace": "{{workspace}}", "source": "diff", "diff_file": "{{diff_file}}", "head_sha": "{{head_sha}}", "github": "{{github}}"}}]
        }
      ]
    },
    {
      "id": "review",
      "display_name": "Review",
      "description": "One dispatch per planned unit, `role_id: \"reviewer\"` — the SAME `crawl.unit` kind `crawl.json`'s own `crawl` phase uses, dispatching a different role because #2310 P4c made the role a Task property the kind reads rather than a hardcoded `\"crawler\"`.",
      "tasks": [
        {"id": "unit-intent-vs-diff", "display_name": "Review: Intent vs diff", "description": "One task per unit `plan-intent-vs-diff` produced.", "role_id": "reviewer", "depends_on": ["plan-intent-vs-diff"], "grow": {"from": "plan-intent-vs-diff", "items": "units", "id": "{{item.id}}", "config": {"plan": "{{from.output}}", "unit": "{{item.id}}", "rule": "intent-vs-diff", "intent_file": "{{intent_file}}", "draws": "{{draws}}"}}, "steps": [{"id": "unit-intent-vs-diff-step", "kind": "crawl.unit", "config": {}}]},
        {"id": "unit-existing-solution", "display_name": "Review: Existing solution", "description": "One task per unit `plan-existing-solution` produced.", "role_id": "reviewer", "depends_on": ["plan-existing-solution"], "grow": {"from": "plan-existing-solution", "items": "units", "id": "{{item.id}}", "config": {"plan": "{{from.output}}", "unit": "{{item.id}}", "rule": "existing-solution", "intent_file": "{{intent_file}}", "draws": "{{draws}}"}}, "steps": [{"id": "unit-existing-solution-step", "kind": "crawl.unit", "config": {}}]},
        {"id": "unit-shared-symbol-callers", "display_name": "Review: Shared symbol callers", "description": "One task per unit `plan-shared-symbol-callers` produced.", "role_id": "reviewer", "depends_on": ["plan-shared-symbol-callers"], "grow": {"from": "plan-shared-symbol-callers", "items": "units", "id": "{{item.id}}", "config": {"plan": "{{from.output}}", "unit": "{{item.id}}", "rule": "shared-symbol-callers", "intent_file": "{{intent_file}}", "draws": "{{draws}}"}}, "steps": [{"id": "unit-shared-symbol-callers-step", "kind": "crawl.unit", "config": {}}]},
        {"id": "unit-union-vs-enum", "display_name": "Review: Union vs enum", "description": "One task per unit `plan-union-vs-enum` produced.", "role_id": "reviewer", "depends_on": ["plan-union-vs-enum"], "grow": {"from": "plan-union-vs-enum", "items": "units", "id": "{{item.id}}", "config": {"plan": "{{from.output}}", "unit": "{{item.id}}", "rule": "union-vs-enum", "intent_file": "{{intent_file}}", "draws": "{{draws}}"}}, "steps": [{"id": "unit-union-vs-enum-step", "kind": "crawl.unit", "config": {}}]},
        {"id": "unit-test-gap", "display_name": "Review: Test gap", "description": "One task per unit `plan-test-gap` produced.", "role_id": "reviewer", "depends_on": ["plan-test-gap"], "grow": {"from": "plan-test-gap", "items": "units", "id": "{{item.id}}", "config": {"plan": "{{from.output}}", "unit": "{{item.id}}", "rule": "test-gap", "intent_file": "{{intent_file}}", "draws": "{{draws}}"}}, "steps": [{"id": "unit-test-gap-step", "kind": "crawl.unit", "config": {}}]},
        {"id": "unit-swallowed-error", "display_name": "Review: Swallowed errors", "description": "One task per unit `plan-swallowed-error` produced.", "role_id": "reviewer", "depends_on": ["plan-swallowed-error"], "grow": {"from": "plan-swallowed-error", "items": "units", "id": "{{item.id}}", "config": {"plan": "{{from.output}}", "unit": "{{item.id}}", "rule": "swallowed-error", "intent_file": "{{intent_file}}", "draws": "{{draws}}"}}, "steps": [{"id": "unit-swallowed-error-step", "kind": "crawl.unit", "config": {}}]},
        {"id": "unit-unnamed-predicate", "display_name": "Review: Unnamed predicates", "description": "One task per unit `plan-unnamed-predicate` produced.", "role_id": "reviewer", "depends_on": ["plan-unnamed-predicate"], "grow": {"from": "plan-unnamed-predicate", "items": "units", "id": "{{item.id}}", "config": {"plan": "{{from.output}}", "unit": "{{item.id}}", "rule": "unnamed-predicate", "intent_file": "{{intent_file}}", "draws": "{{draws}}"}}, "steps": [{"id": "unit-unnamed-predicate-step", "kind": "crawl.unit", "config": {}}]}
      ]
    },
    {
      "id": "summarize",
      "display_name": "Summarize",
      "description": "This run's totals, read back through every `crawl.unit` step's own outcome — the SAME `crawl.summary` kind `crawl.json` uses.",
      "tasks": [
        {
          "id": "summary",
          "display_name": "Summary",
          "description": "Total this review's units, findings and tokens.",
          "steps": [{"id": "summary-step", "kind": "crawl.summary", "config": {}}]
        }
      ]
    },
    {
      "id": "create-mods",
      "display_name": "Create mods",
      "description": "Reuses crawl's create-mods `create-mod-step` verbatim (DESIGN.md \"Review reuses the create-mods phase\") — that step's shape is byte-identical to `crawl.json`'s own. `crawl.json`'s create-mods never runs tests (findings are proposals over a read-only tree, no test target implied); a review's mod SHOULD run the changed files' own test target before it counts as confirmed (DESIGN.md \"the changed files name the test targets, which is what makes confirmation cheap enough to do per finding\") — the ONE way this task's shape diverges from crawl's: a second step, `gate-step` (`mods.gate`, #2310 P4c-2b), runs `test_command` (when the launch set one) against `{{item.tree_root}}` and records `gate_passed`/`gate_skipped_reason` on every mod naming this task's finding.",
      "tasks": [
        {
          "id": "create-mod",
          "display_name": "Create mod: propose the change one finding describes",
          "description": "Dispatch one `coder` per finding — one task per entry in the summary's `finding_refs`.",
          "role_id": "coder",
          "depends_on": ["summary"],
          "grow": {
            "from": "summary",
            "items": "finding_refs",
            "id": "{{item.id}}",
            "config": {
              "workdir": "{{item.tree_root}}",
              "brief_refs": [{"kind": "finding", "key": "{{item.key}}"}],
              "message": "A finding recorded earlier in this run is appended below. Your job is to PROPOSE the change that finding describes, as a mod. The workspace you have been given is the tree the finding was observed in, mounted read-only: you cannot edit it, and you are not meant to.\n\n1. Read the file the finding names, around the line it names.\n2. If the finding does not hold against what you read, record nothing and say why.\n3. Otherwise write the smallest change that resolves it as a unified diff against the file as it is, and call `create_mod` with: `kit` = one sentence saying what to apply and the exact diff; `for` = the finding's key exactly as the block below spells it.\n\nDo not run tests, and do not touch anything the finding does not name. The mod is your product; someone else integrates it.",
              "for_key": "{{item.key}}",
              "test_command": "{{test_command}}"
            }
          },
          "steps": [
            {"id": "create-mod-step", "kind": "dispatch.internal", "config": {}},
            {"id": "gate-step", "kind": "mods.gate", "config": {}}
          ]
        }
      ]
    },
    {
      "id": "deliver",
      "display_name": "Deliver",
      "description": "#2310 P4c-2b closes the gap the `create-mods` phase's own description names: `records.gather` reads THIS mission's own finding/mod stores (scoped by mission id — the way `finding list --mission`/`mod list --mission` resolve it) plus `diff_file`, into the typed shape `deliver.github_review` renders. Two steps of ONE task, `records-gather-step` then `deliver-step` — the SAME same-task-predecessor `Step.output` -> `input` wiring (`scheduler::gather_inputs`) every other multi-step task in this codebase uses, never a new mechanism. `run_on: [\"complete\",\"error\"]` on the task documents the intent DESIGN.md's own retirement finding names (\"an errored run rendered nothing... None were in a step\") — an errored `create-mods` phase must not silence delivery; this task declares no `depends_on` at all (an edge naming `create-mod`, a grow TEMPLATE, is refused at validation — see `mission_config::interpret`'s own doc), so ordering comes from `create-mods` being an earlier PHASE in this document, the same phase-ordering `crawl.json`'s `summarize` phase relies on to see every grown `crawl.unit`'s results with no `depends_on` of its own.",
      "tasks": [
        {
          "id": "deliver",
          "display_name": "Deliver: gather this run's records and render a GitHub review",
          "description": "Gather then deliver — see this phase's own description.",
          "run_on": ["complete", "error"],
          "steps": [
            {"id": "records-gather-step", "kind": "records.gather", "config": {"diff_file": "{{diff_file}}"}},
            {"id": "deliver-step", "kind": "deliver.github_review", "config": {"emit": "{{emit}}", "attribution": "{{attribution}}"}}
          ]
        }
      ]
    }
  ]
}
