{
  "_comment": "(#1959) First crawler workload. Scans a bounded Rust scope for ONE named pattern and records each match via the report_finding tool. Read-only by role palette. The point of this workload is MEASUREMENT — finding rate, evidence validity, and whether a local model reliably calls a schema'd tool at all — not the findings themselves. Register the scope via `darkmux lab fixture register`; the ${SANDBOX_DIR} placeholder resolves to the per-run clone.",

  "workload": {
    "id": "crawl-error-discard",
    "provider": "coding-task",
    "role": "crawler",
    "description": "Crawl a Rust scope for Results discarded via `let _ = ...`, reporting each through report_finding.",
    "prompt": "Your scope is the directory ${SANDBOX_DIR}/crates/darkmux-flow/src.\n\nYour pattern is: **a Result whose error is discarded with `let _ = ...`**.\n\nIn Rust, `let _ = some_call();` silently throws away whatever the call returned. When the returned value is a `Result`, an error can occur and nothing — no log line, no metric, no propagation — records that it happened. The operation appears to succeed.\n\nA line matches this pattern when ALL of these hold:\n\n- the line assigns to `_` using `let _ =`\n- the right-hand side returns a `Result` — a fallible call: file I/O, a write, a send, a parse, a spawn, a lock\n- nothing on the surrounding lines logs, counts, or otherwise notices the failure\n\nA line does NOT match when:\n\n- the discarded value is not a `Result` — dropping a guard, a handle, or a join handle for its side effect is normal and correct\n- the failure is deliberately ignored with a comment right there explaining why that is safe\n\nWork through the files in that directory. For each match, call `report_finding` with these five keys exactly: `file`, `line`, `pattern`, `evidence`, `why`. `evidence` must be the source line copied verbatim, and `line` must be where it appears.\n\nBe strict. A `let _ =` you have not confirmed returns a Result is not a finding. Open the file and read it before you report.\n\nWhen you are done, say which files you examined, which you did not get to, and whether you covered the whole directory.",

    "requiresExternalSandbox": true,
    "requires_fixture": "rust-crawl-scope@1.0",

    "verify": {
      "must_contain": [],
      "must_not_contain": []
    }
  }
}
