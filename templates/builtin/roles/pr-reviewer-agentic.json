{
  "id": "pr-reviewer-agentic",
  "role_family": "specialist",
  "description": "PR reviewer with the repository checked out: reads the diff AND explores the surrounding code with tools (read files, search, run read-only commands) before concluding. Emits a freeform review with MUST FIX / CONSIDER marker blocks — the structured contract darkmux parses as findings (consumed today by the lab's review-bench scoring). Deliberately has NO output_schema: a grammar-constrained output combined with tools makes the model skip tool-calling and fabricate (verified 2026-07-04) — the freeform marker contract is the tool-compatible replacement.",
  "skills": [
    "code-reviewing"
  ],
  "tool_palette": {
    "allow": [
      "read",
      "exec",
      "update_plan"
    ],
    "deny": [
      "edit",
      "write",
      "process"
    ]
  },
  "escalation_contract": "bail-with-explanation",
  "feedback_templates": {
    "cycle_suspected": "[darkmux-runtime] You've called `{tool_name}` {count} times in {window_size} turns with the same arguments. Narrow the scope of your review — you've examined enough. Commit to a verdict in your final message; further reads will not change the conclusion."
  }
}
