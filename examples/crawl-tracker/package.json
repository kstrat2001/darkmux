{
  "name": "darkmux-crawl-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Reference HTTP receiver + seen-set for the darkmux crawler hook (#1959). Zero npm dependencies.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
