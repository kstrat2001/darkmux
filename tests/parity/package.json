{
  "name": "darkmux-viewer-parity",
  "version": "0.0.0",
  "private": true,
  "description": "Packet 0a of the viewer -> React port. The PARITY HARNESS: records the legacy viewer's (crates/darkmux-serve/assets/viewer.html) observable behavior against a live daemon into sanitized fixtures, then extracts normalized golden text per lens. The React port is graded against these goldens, not against a description of them. Self-contained; does not touch tests/e2e.",
  "scripts": {
    "record": "bun run record.mjs",
    "extract": "playwright test extract.spec.ts",
    "rebaseline": "bun run extract",
    "verify": "bun run verify-goldens.mjs",
    "redprove": "playwright test redprove.spec.ts",
    "next-parity": "playwright test --config next-playwright.config.js",
    "determinism": "bun run determinism.mjs",
    "tripwire": "bun run tripwire.mjs",
    "check": "bun run verify && bun run redprove && bun run determinism && bun run tripwire"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
