{
  "name": "darkmux-viewer-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Headless browser e2e gate for the observability viewer (crates/darkmux-serve/assets/viewer.html). Dev-only; not part of the shipped binary.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
