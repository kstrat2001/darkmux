This sentinel marks a darkmux skill-bundle source directory.
Used by `darkmux init` (which installs and refreshes the bundled skills) to confirm the source is genuinely darkmux's bundle, not an unrelated `skills/` directory that happens to be in the user's cwd.
