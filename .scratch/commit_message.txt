feat(acp): radio answering seat — grounded answers, artifact shelf, radio config, session load + idle exit (#1698)

Packet B2 (the last packet of the radio arc): a router refusal on the
panel no-slash channel and the `darkmux radio` CLI verb now routes to
a grounded, in-persona answering seat instead of printing the bare
refusal + command listing. Adds the deterministic zero-model grounding
assembler (catalog, live config surface, mission-board summary, help
text, the session's artifact shelf, and a local mission deep artifact
when named), the per-session artifact shelf, the radio-host persona
role with a humor dial substituted at assembly time via a new
`DispatchOpts.system_prompt_override` field, the `radio` config block,
session config-option pickers (radio host + humor), minimal
`session/load`, and idle self-exit for the `darkmux acp` process.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01CW1oqeju57ukFasqevb1p2
