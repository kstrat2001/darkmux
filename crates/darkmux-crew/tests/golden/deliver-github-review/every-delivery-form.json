{
  "mode": "review",
  "review": {
    "event": "COMMENT",
    "body": "### darkmux review\n\nreview ran: 2 of 3 rules reviewed, 3/4 hunks covered, 8 finding(s) considered, 2 refused. Not attempted: architectural review. This review checks a fixed set of rules; it is not a full design review. 2 proposed changes not verified — the gate ran and did not pass; `darkmux mod show mod-1-abcdef` | not yet gated; `darkmux mod show mod-1-abcdef`.\n\n**A compound condition encodes a domain rule that has no name and cannot be tested on its own** `unnamed-predicate`\n- `src/other.ts:5` — unrelated change. The change is written out rather than as a patch, so it is quoted here:\n\n```\nthe patch text\n```\n\n**A shared function or type's signature or behavior changed** `shared-symbol-callers`\n- 1 finding could not be anchored to a line.\n\n**A new routine looks re-implemented rather than reused** `existing-solution`\n- 1 finding could not be anchored to a line.\n\n_Advisory, not a merge gate._",
    "comments": [
      {
        "path": "src/a.ts",
        "line": 2,
        "side": "RIGHT",
        "body": "- reimplements clamp()\n\n```suggestion\n  const x = clamp(1);\n```"
      },
      {
        "path": "src/a.ts",
        "line": 2,
        "side": "RIGHT",
        "body": "- might duplicate an existing helper\n\n`unnamed-predicate`"
      },
      {
        "path": "src/a.ts",
        "line": 2,
        "side": "RIGHT",
        "body": "- a gate-failed claim\n\n`union-vs-enum`\n\n1 proposed change not verified — the gate ran and did not pass; `darkmux mod show mod-1-abcdef`"
      },
      {
        "path": "src/a.ts",
        "line": 2,
        "side": "RIGHT",
        "body": "- a mod exists but nothing ever gated it\n\n`swallowed-error`\n\n1 proposed change not verified — not yet gated; `darkmux mod show mod-1-abcdef`"
      },
      {
        "path": "src/a.ts",
        "line": 1,
        "side": "RIGHT",
        "body": "- this union duplicates the Status enum\n\n```suggestion\nfunction f(): Status {\n```"
      }
    ]
  },
  "fallback_comment": "review ran: 2 of 3 rules reviewed, 3/4 hunks covered, 8 finding(s) considered, 2 refused. Not attempted: architectural review. This review checks a fixed set of rules; it is not a full design review. 2 proposed changes not verified — the gate ran and did not pass; `darkmux mod show mod-1-abcdef` | not yet gated; `darkmux mod show mod-1-abcdef`.\n\n- `src/a.ts:2` — reimplements clamp().\n- `src/other.ts:5` — unrelated change.\n- `src/a.ts:2` — might duplicate an existing helper.\n- `src/a.ts:2` — a gate-failed claim.\n- `src/mw.ts:10` — shared auth changed.\n- `src/x.ts:1` — did you check whether the repo already has this.\n- `src/a.ts:2` — a mod exists but nothing ever gated it.\n- `src/a.ts:1` — this union duplicates the Status enum."
}
