{
  "mode": "review",
  "review": {
    "event": "COMMENT",
    "body": "### darkmux review\n\nreview ran: 2 of 3 rules reviewed, 3/4 hunks covered, 8 finding(s) considered, 2 refused. Not attempted: architectural review. This review checks a fixed set of rules; it is not a full design review.\n\n**A compound condition encodes a domain rule that has no name and cannot be tested on its own** `unnamed-predicate`\n- `src/other.ts:5` — unrelated change. The change is written out rather than as a patch, so it is quoted here:\n\n```\nthe patch text\n```\n\n_Advisory, not a merge gate._",
    "comments": [
      {
        "path": "src/a.ts",
        "line": 2,
        "side": "RIGHT",
        "body": "- reimplements clamp()\n\n```suggestion\n  const x = clamp(1);\n```"
      },
      {
        "path": "src/a.ts",
        "line": 2,
        "side": "RIGHT",
        "body": "- might duplicate an existing helper\n\n`unnamed-predicate`"
      },
      {
        "path": "src/a.ts",
        "line": 2,
        "side": "RIGHT",
        "body": "- a gate-failed claim\n\n`union-vs-enum`"
      },
      {
        "path": "src/mw.ts",
        "line": 10,
        "side": "RIGHT",
        "body": "- shared auth changed\n\n`shared-symbol-callers`"
      },
      {
        "path": "src/x.ts",
        "line": 1,
        "side": "RIGHT",
        "body": "- did you check whether the repo already has this\n\n`existing-solution`"
      },
      {
        "path": "src/a.ts",
        "line": 2,
        "side": "RIGHT",
        "body": "- a mod exists but nothing ever gated it\n\n`swallowed-error`"
      },
      {
        "path": "src/a.ts",
        "line": 1,
        "side": "RIGHT",
        "body": "- this union duplicates the Status enum\n\n```suggestion\nfunction f(): Status {\n```"
      }
    ]
  }
}
